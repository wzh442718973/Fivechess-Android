package com.lucking.shine.tiamshow.game;

import com.lucking.shine.issued.YExampleBackObject;

import com.lucking.shine.issued.EBaseNotificationObject;

import com.lucking.shine.issued.XNClientLocalObject;


import java.util.ArrayList;


import java.util.Random;


import java.util.HashMap;



import java.io.Serializable;





public class MXUpdateFragment implements Serializable{
private boolean enbaleSaveLeftCompleted = false;
private HashMap<String,Integer> broadcastStringMap;


 YExampleBackObject closeObject;
 EBaseNotificationObject lastObject;
private XNClientLocalObject messageModel;

    private static final long serialVersionUID = 4958644533922626552L;
    
    public int x;
    public int y;

    public MXUpdateFragment() {
    }


    

private boolean layoutDataWait(String fileContext, double backgroundCompare, int chevronPlayer) {
     double whitePaint = 6.0;
     ArrayList<String> dimensClass_am = new ArrayList();
     double entrySelector = 7643.0;
    boolean istreamwrapperOvflColon = false;
    whitePaint *= whitePaint;
    whitePaint += entrySelector;
    istreamwrapperOvflColon = whitePaint > 7;
    entrySelector += whitePaint;
    entrySelector *= entrySelector;
    istreamwrapperOvflColon = entrySelector > 28;

    return istreamwrapperOvflColon;

}



public int getX() {

         String xchg_r = "deshake";

boolean localizationRgbx =  this.layoutDataWait(xchg_r,9883.0,75);

      if (localizationRgbx) {
          System.out.println("ok");
      }



        if (this.closeObject != null) {
        }
        if (this.lastObject != null) {
        }
        if (null != this.messageModel) {
        }

        return x;
    }

    

private boolean cancelCorrect(long thirdRestore, ArrayList<Long> sideUpload) {
     double enabledTemp = 2675.0;
     double timerAgent = 2737.0;
     String gradleRegister_b = "ids";
    boolean residenceBold = false;
    enabledTemp = 6743;
    residenceBold = enabledTemp > 17;
    timerAgent *= 97;
    residenceBold = timerAgent > 23;

    return residenceBold;

}



public MXUpdateFragment setX(int x) {

         ArrayList prefixed_l = new ArrayList();

boolean beingEllipse =  this.cancelCorrect(9776L,prefixed_l);

      if (!beingEllipse) {
          System.out.println("ok");
      }



        if (this.closeObject != null) {
            
int invalidatedRequirements =  this.closeObject.mainOrientation(new HashMap(),9852L,8686);

   if (invalidatedRequirements > 1) {
      for (int q_h = 0; q_h < invalidatedRequirements; q_h++) {
          if (q_h == 1) {
              System.out.println(q_h);
              break;
          }
      }
  }

            
int tollAmounts =  this.closeObject.progressFocus();

      if (tollAmounts >= 64) {
             System.out.println(tollAmounts);
      }

        }
        if (this.lastObject != null) {
            
float weekLittle =  this.lastObject.achievementEnabled(8500,1976);

      System.out.println(weekLittle);

            
String sigmaFctl =  this.lastObject.freeReferrer(new HashMap());

      int sigmaFctl_len = sigmaFctl.length();
      if (sigmaFctl == "available") {
              System.out.println(sigmaFctl);
      }

        }
        if (null != this.messageModel) {
            
double bindDitable =  this.messageModel.pageEntry(2135L);

      System.out.println(bindDitable);

            ArrayList myself_s = new ArrayList();
ArrayList libspeex_l = new ArrayList();

boolean eaidctOneinch =  this.messageModel.removeNative(myself_s,libspeex_l);

      if (!eaidctOneinch) {
          System.out.println("ok");
      }

        }

        this.x = x;
            boolean activityB = false;
             if (activityB) {}
        return this;
    }

    

private int finishedAttach(long resultGoogle, float testAdjust, HashMap<String,Float> override_qtFunction) {
     float navigationResult = 3845.0f;
     int chevronAndroid = 8665;
     long thirdRecycler = 7507L;
    int hexRecpTestnets = 0;
    navigationResult -= 27;
    chevronAndroid *= 91;
    hexRecpTestnets *= chevronAndroid;
    thirdRecycler = 1134;

    return hexRecpTestnets;

}



public int getY() {

         
int recycleResetbuf =  this.finishedAttach(1763L,1349.0f,new HashMap());

      if (recycleResetbuf >= 80) {
             System.out.println(recycleResetbuf);
      }



        if (null != this.closeObject) {
            String cinvideo_p = "insets";

long xingFolders =  this.closeObject.window_ca(cinvideo_p);

      System.out.println(xingFolders);

            
double testerMigrated =  this.closeObject.descriptiveRangeMeasure(false);

      if (testerMigrated == 68) {
             System.out.println(testerMigrated);
      }

        }
        if (this.lastObject != null) {
            
double pastedSolidity =  this.lastObject.checkUsable(8415,734.0,true);

      if (pastedSolidity < 3) {
             System.out.println(pastedSolidity);
      }

            String rounds_i = "freeze";

float inputxRng =  this.lastObject.pointsOrientation(true,rounds_i);

      System.out.println(inputxRng);

        }
        if (null != this.messageModel) {
            
HashMap bnmpiSuch =  this.messageModel.descriptiveStartedMethod_nn(false,4468.0,9159);

      int bnmpiSuch_len = bnmpiSuch.size();
      for(Object obj_t : bnmpiSuch.entrySet()) {
          HashMap.Entry<String, Object> entry = (HashMap.Entry<String, Object>)obj_t;
          System.out.println(entry.getKey());
          System.out.println(entry.getValue());
      }

            ArrayList minimizeable_a = new ArrayList();

HashMap rubyNsse =  this.messageModel.correctHash(792.0,3950.0,minimizeable_a);

      ArrayList _rubyNssetemp = new ArrayList(rubyNsse.keySet());
      for(int index_9 = 0; index_9 < _rubyNssetemp.size(); index_9++) {
          Object key_index_9 = _rubyNssetemp.get(index_9);
          Object value_index_9 = rubyNsse.get(key_index_9);
          if (index_9  <=  80) {
              System.out.println(key_index_9);
              System.out.println(value_index_9);
              break;
          }
      }
      int rubyNsse_len = rubyNsse.size();

        }

        return y;
    }

    

private String size_j2() {
     long workaroundCreate = 4832L;
     int computeEvent = 1691;
     int drawBaseline = 3100;
    String dlsymRewrittenVelocity = "ask";
    if (workaroundCreate <= 128 && workaroundCreate >= -128){
    int zalo_v = Math.min(1, new Random().nextInt(87)) % dlsymRewrittenVelocity.length();
        dlsymRewrittenVelocity += workaroundCreate + "";
    }
    if (computeEvent <= 128 && computeEvent >= -128){
    int splash_e = Math.min(1, new Random().nextInt(84)) % dlsymRewrittenVelocity.length();
        dlsymRewrittenVelocity += computeEvent + "";
    }
    if (drawBaseline >= -128 && drawBaseline <= 128){
    int post_c = Math.min(1, new Random().nextInt(65)) % dlsymRewrittenVelocity.length();
        dlsymRewrittenVelocity += drawBaseline + "";
    }

    return dlsymRewrittenVelocity;

}



public MXUpdateFragment setY(int y) {

         
String avgyObject =  this.size_j2();

      System.out.println(avgyObject);
      int avgyObject_len = avgyObject.length();



        if (this.closeObject != null) {
            
double certpolEncodingb =  this.closeObject.this_zdThis_sc(9582.0f);

      System.out.println(certpolEncodingb);

        }
        if (null != this.lastObject) {
            
boolean compCardinality =  this.lastObject.metaTouchType_0();

      if (compCardinality) {
      }

            
double ultraAltivec =  this.lastObject.navigationComputerGoto_s(2262L,false);

      if (ultraAltivec != 0) {
             System.out.println(ultraAltivec);
      }

        }
        if (null != this.messageModel) {
            
int invitationsXlabelheight =  this.messageModel.wallWhatsLoading(1033,552);

      if (invitationsXlabelheight == 56) {
             System.out.println(invitationsXlabelheight);
      }

            
String variancexhMmco =  this.messageModel.logonCreated(new HashMap(),new HashMap(),true);

      int variancexhMmco_len = variancexhMmco.length();
      System.out.println(variancexhMmco);

        }

        this.y = y;
            double instrumentedd = 7071.0;
             while (instrumentedd < 40) { break; }
        return this;
    }

    public MXUpdateFragment(int x, int y) {
        this.x = x;
        this.y = y;
    }


    @Override
    public boolean equals(Object o) {
        if (null != this.lastObject) {
            
double readcbModification =  this.lastObject.finishedLoadLogout(true,2949.0f,4692L);

      System.out.println(readcbModification);

            ArrayList beneath_g = new ArrayList();

ArrayList importableHuff =  this.lastObject.progressDescriptiveMain(beneath_g,6303);

      for(int index_x = 0; index_x < importableHuff.size(); index_x++) {
          Object obj_index_x = importableHuff.get(index_x);
          if (index_x  >=  48) {
                System.out.println(obj_index_x);
          }
      }
      int importableHuff_len = importableHuff.size();

        }
        if (null != this.messageModel) {
            ArrayList decoderinit_g = new ArrayList();

float chunkOcsp =  this.messageModel.callAddition(4484.0,decoderinit_g);

      if (chunkOcsp < 15) {
             System.out.println(chunkOcsp);
      }

            
ArrayList mergeableElementwise =  this.messageModel.downOrientation(new HashMap(),new HashMap());

      int mergeableElementwise_len = mergeableElementwise.size();
      for(int index_p = 0; index_p < mergeableElementwise.size(); index_p++) {
          Object obj_index_p = mergeableElementwise.get(index_p);
          if (index_p  !=  26) {
                System.out.println(obj_index_p);
          }
      }

        }

        if (this == o) return true;
        if (o == null || getClass() != o.getClass()) return false;

        MXUpdateFragment point = (MXUpdateFragment) o;
            HashMap<String,Float> alertk = new HashMap<String,Float>();
     alertk.put("recording", 746.0f);
     alertk.put("jpegint", 599.0f);
     alertk.put("pending", 530.0f);
     alertk.put("aybri", 87.0f);
     alertk.put("utc", 377.0f);

        if (x != point.x) return false;
        return y == point.y;
    }

    @Override
    public int hashCode() {
        if (null != this.lastObject) {
            
long proresdataDum =  this.lastObject.createdExecuteEnabled(357.0f,9954.0f,3254.0);

      if (proresdataDum == 55) {
             System.out.println(proresdataDum);
      }

        }
        if (null != this.messageModel) {
            
ArrayList nameserverSavepoint =  this.messageModel.currentConnected(3804,5531.0f);

      int nameserverSavepoint_len = nameserverSavepoint.size();
      for(Object obj2 : nameserverSavepoint) {
          System.out.println(obj2);
      }

            
boolean dampingFaxcompr =  this.messageModel.time_kCameraHuman();

      if (dampingFaxcompr) {
          System.out.println("update_m");
      }

        }

        int result = x;
            boolean filea = true;
        result = 31 * result + y;
            double servicew = 3195.0;
             if (servicew == 163) {}
        return result;
    }

    

private boolean pointFullscreenFailure() {
     ArrayList<Float> relyEmpty = new ArrayList();
     ArrayList<Float> mapSecurity = new ArrayList();
     String appChannel = "ntsc";
     double stringsExample = 6273.0;
    boolean dcblockEditSqlitesession = false;
    stringsExample *= 55;
    dcblockEditSqlitesession = stringsExample > 47;

    return dcblockEditSqlitesession;

}



@Override
    public String toString() {

         
boolean convColorful =  this.pointFullscreenFailure();

      if (!convColorful) {
      }



        return "MXUpdateFragment("+x+","+y+")";
    }
}