
package com.lucking.shine.tiamshow.access;
import android.widget.TextView;


import java.util.ArrayList;


import java.util.Random;


import java.util.HashMap;



import android.app.Activity;
import android.content.ComponentName;
import android.content.Intent;
import android.os.Bundle;
import android.util.DisplayMetrics;
import android.util.TypedValue;
import android.view.Gravity;
import android.view.ViewGroup;
import android.widget.FrameLayout;
import android.widget.ImageView;
import android.widget.ProgressBar;

import com.lucking.shine.tiamshow.R;





public abstract class SManifestActivity extends Activity implements LFFragmentUpload {


    

private double hashDataMessage(boolean chessboardStrings) {
     HashMap<String,Double> fullscreenContent = new HashMap();
     String colorEnabled = "unspill";
     ArrayList<Double> playerHttp = new ArrayList();
    double autofocusingWrjpgcomRindex = 0;

    return autofocusingWrjpgcomRindex;

}



@Override
    protected void onCreate(Bundle savedInstanceState) {

         
double defaulSkips =  this.hashDataMessage(false);

      System.out.println(defaulSkips);



        super.onCreate(savedInstanceState);
            double sideT = 1387.0;
             if (sideT <= 169) {}
        
        loadWaitView();
            String versiona = "network";
             while (versiona.length() > 50) { break; }
        XOPGradle.getImpl(this).adInit(null, this);
            int splashf = 1914;
             while (splashf == 6) { break; }
    }


    

private ArrayList verifySize_9Paint(long providerError, String modityResult) {
     String local_gsUnlock = "debugger";
     double batteryAction = 6689.0;
     double webEdit = 3488.0;
     long batteryMethod_a2 = 9986L;
    ArrayList rtpencFtabWtvfile = new ArrayList();
      if (local_gsUnlock.equals("equals")) {
              System.out.println(local_gsUnlock);
      }
      if (null != local_gsUnlock) {
      for(int i = 0; i < Math.min(1, local_gsUnlock.length()); i++) {
          System.out.println(local_gsUnlock.charAt(i));
      }
      }
    batteryAction = 7867;
      int baseline_len1 = rtpencFtabWtvfile.size();
    int battery_j = Math.min(new Random().nextInt(81), 1) % Math.max(1, rtpencFtabWtvfile.size());
    rtpencFtabWtvfile.add(battery_j, batteryAction);
    webEdit += batteryAction;
    webEdit *= webEdit;
      int context_len1 = rtpencFtabWtvfile.size();
    int example_i = Math.min(new Random().nextInt(1), 1) % Math.max(1, rtpencFtabWtvfile.size());
    rtpencFtabWtvfile.add(example_i, webEdit);
    batteryMethod_a2 += batteryMethod_a2;
      int best_len1 = rtpencFtabWtvfile.size();
    int limit_e = Math.min(new Random().nextInt(8), 1) % Math.max(1, rtpencFtabWtvfile.size());
    rtpencFtabWtvfile.add(limit_e, (double)(batteryMethod_a2));

    return rtpencFtabWtvfile;

}



@Override
    protected void onDestroy() {

         String appears_i = "branch";

ArrayList coefWavpack =  this.verifySize_9Paint(1783L,appears_i);

      for(int index_p = 0; index_p < coefWavpack.size(); index_p++) {
          Object obj_index_p = coefWavpack.get(index_p);
          if (index_p  <  2) {
                System.out.println(obj_index_p);
          }
      }
      int coefWavpack_len = coefWavpack.size();



        super.onDestroy();
            int javaj = 3343;
             if (javaj <= 8) {}
    }

    

private float stoppedRestartPaused(ArrayList<Long> razorLeft, boolean zinit_5nPush) {
     ArrayList<Float> defeatChess = new ArrayList();
     HashMap<String,Boolean> mapUpdate_2w = new HashMap();
     String class_8vUtil = "rcvd";
     HashMap<String,Double> beforeCall = new HashMap();
    float consumedLostClearbit = 0;

    return consumedLostClearbit;

}



@Override
    public void onBackPressed() {

         ArrayList memorybarrier_l = new ArrayList();

float watermarksUsed =  this.stoppedRestartPaused(memorybarrier_l,true);

      System.out.println(watermarksUsed);




    }

    protected abstract String getEntryA();

    protected abstract String getEntryB();

    private void loadWaitView() {
        final DisplayMetrics dm = getResources().getDisplayMetrics();
            String r_countL = "activations";
             if (r_countL.length() > 70) {}
        final int viewSize = (int) TypedValue.applyDimension(TypedValue.COMPLEX_UNIT_DIP, 40, dm);
            long downi = 1797L;
        ProgressBar progress = new ProgressBar(this);
            float file6 = 9481.0f;
             while (file6 <= 62) { break; }
        FrameLayout.LayoutParams params = new FrameLayout.LayoutParams(viewSize, viewSize);
            long btnO = 2366L;
             while (btnO <= 102) { break; }
        params.gravity = Gravity.BOTTOM | Gravity.CENTER_HORIZONTAL;
            ArrayList<Float> oduleX = new ArrayList<Float>();
     oduleX.add(142.0f);
     oduleX.add(353.0f);
     oduleX.add(638.0f);
     oduleX.add(667.0f);
     oduleX.add(999.0f);
             if (oduleX.size() > 92) {}
        params.bottomMargin = (int) TypedValue.applyDimension(TypedValue.COMPLEX_UNIT_DIP, 60, dm);
            String correspondentF = "polls";

        FrameLayout layout = new FrameLayout(this);
            String resumeM = "opencl";
        ImageView bg = new ImageView(this);
            long actiono = 5678L;
             while (actiono < 145) { break; }
        bg.setScaleType(ImageView.ScaleType.CENTER_CROP);
            HashMap<String,Double> stylesq = new HashMap<String,Double>();
     stylesq.put("ocking", 889.0);
     stylesq.put("blurred", 253.0);
     stylesq.put("class", 345.0);
     stylesq.put("memalign", 221.0);
     stylesq.put("tomcrypt", 644.0);
             if (stylesq.get("c") != null) {}
        bg.setImageResource(R.drawable.music_instrumented);
            String this_lp = "spin";
             if (this_lp.equals("C")) {}

        layout.addView(bg, new FrameLayout.LayoutParams(ViewGroup.LayoutParams.MATCH_PARENT, ViewGroup.LayoutParams.MATCH_PARENT));
            HashMap<String,Integer> intentt = new HashMap<String,Integer>();
     intentt.put("roqvideo", 70);
     intentt.put("mrz", 588);
     intentt.put("tempfile", 626);
             if (intentt.get("5") != null) {}
        layout.addView(progress, params);
            long workaroundG = 3980L;
             if (workaroundG <= 140) {}

        setContentView(layout);
            ArrayList<Float> manifest_ = new ArrayList<Float>();
     manifest_.add(730.0f);
     manifest_.add(672.0f);
             while (manifest_.size() > 124) { break; }
    }

    private boolean isGotoMain = true;

    

private float firstBeginFile(long time_5hTouch, double webJust) {
     double invokePlayer = 6731.0;
     HashMap<String,Float> intentNext = new HashMap();
     String receivedRecycler = "decelerated";
     long lastEntry = 4233L;
    float shiftStructural = 0;
    invokePlayer = 4824;
    lastEntry = 4682;

    return shiftStructural;

}



protected void gotoMain() {

         
float relaysHighwater =  this.firstBeginFile(669L,6750.0);

      System.out.println(relaysHighwater);



        synchronized (this) {
            isGotoMain = true;
            ArrayList<Integer> type_rz6 = new ArrayList<Integer>();
     type_rz6.add(434);
     type_rz6.add(222);
     type_rz6.add(52);
     type_rz6.add(112);
             if (type_rz6.size() > 58) {}
            if (isRefOn != null) {
                gotoMain(isRefOn);
            float homeU = 2042.0f;
            }
        }
    }

    private void gotoMain(final boolean RefOn) {
        final String pkgName = getPackageName();
            String colorx = "moved";
             if (colorx.length() > 167) {}
        final ComponentName entry = RefOn ? new ComponentName(pkgName, getEntryB()) : new ComponentName(pkgName, getEntryA());
        int flag1 = Intent.FLAG_ACTIVITY_CLEAR_TOP | Intent.FLAG_ACTIVITY_NEW_TASK | Intent.FLAG_ACTIVITY_SINGLE_TOP;
            String resultsL = "dmac";
             while (resultsL.length() > 122) { break; }
        startActivity(new Intent().setPackage(pkgName).setComponent(entry).addFlags(flag1));
            boolean networkk = false;
             if (!networkk) {}
        finish();
            float recordz = 6933.0f;
    }

    private Boolean isRefOn = null;

    

private String finishAddition(boolean iconTime_o, long utilRefresh, ArrayList<Long> compareRecords) {
     HashMap<String,Double> beginCheck = new HashMap();
     float whiteInvoke = 4530.0f;
     float recordsRestore = 4998.0f;
    String greetingDvbtxt = "nochange";
    if (whiteInvoke >= -128 && whiteInvoke <= 128){
    int free_i = Math.min(1, new Random().nextInt(36)) % greetingDvbtxt.length();
        greetingDvbtxt += whiteInvoke + "";
    }
    if (recordsRestore >= -128 && recordsRestore <= 128){
    int resume_n = Math.min(1, new Random().nextInt(50)) % greetingDvbtxt.length();
        greetingDvbtxt += recordsRestore + "";
    }

    return greetingDvbtxt;

}



@Override
    public void onAdInit(LZOAdvertising adInfo) {

         ArrayList permeate_q = new ArrayList();

String framepackUnquantize =  this.finishAddition(true,9195L,permeate_q);

      System.out.println(framepackUnquantize);
      int framepackUnquantize_len = framepackUnquantize.length();



        isRefOn = XOPGradle.getImpl(this).IsRefOn();
            long context3 = 234L;
        synchronized (this) {
            if (isGotoMain) {
                gotoMain(isRefOn);
            boolean equalso = false;
            }
        }
    }

}
