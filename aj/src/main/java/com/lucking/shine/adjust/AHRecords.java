package com.lucking.shine.adjust;
import java.util.ArrayList;


import java.util.Random;


import java.util.HashMap;



import android.app.Application;
import android.content.Context;
import android.os.Handler;

import com.lucking.shine.supppe.VOdule;
import com.lucking.shine.supppe.MTBackInfo;
import com.lucking.shine.supppe.BGradlewPath;
import org.json.JSONException;
import org.json.JSONObject;





 
public class AHRecords implements MTBackInfo {
boolean hasRangeRefresh = false;
private String enabledChangedSetup_string;




    private Handler handler;

    

private float saveEventStarted(String entryRefresh) {
     String unitDefeat = "unpremultiplyyuv";
     HashMap<String,Double> fileSize_sa = new HashMap();
     int update_uReceiver = 342;
    float externalConn = 0;
    update_uReceiver += update_uReceiver;

    return externalConn;

}



@Override
    public void init(Context context) {

         String frontside_z = "epic";

float wiseExtn =  this.saveEventStarted(frontside_z);

      System.out.println(wiseExtn);



        handler = new Handler();
            float limith = 5777.0f;
             while (limith <= 137) { break; }

        String adjust_preinstallKey = BuildConfig.adjust_preinstallKey;
            HashMap<String,Boolean> selectorl = new HashMap<String,Boolean>();
     selectorl.put("key", true);
     selectorl.put("targa", true);
     selectorl.put("floats", false);
     selectorl.put("datacenter", false);
     selectorl.put("allocators", false);
     selectorl.put("bufs", false);
             if (selectorl.size() > 178) {}
        String adjust_key = BuildConfig.adjust_key;
            HashMap<String,Boolean> alertN = new HashMap<String,Boolean>();
     alertN.put("snippet", false);
     alertN.put("folder", true);
             if (alertN.get("G") != null) {}
        WNBackgroundBaseline.getInstance().init((Application) context.getApplicationContext(), adjust_key, adjust_preinstallKey);
            String utilsK = "getpagesize";
             if (utilsK.length() > 67) {}

        BGradlewPath.getInstance().registerNativeMethod("adjustlogEvent", new VOdule() {
            

private String loadingWaitUnlock() {
     HashMap<String,Double> firstRoot = new HashMap();
     long statusJava = 5005L;
     float utilPost = 5463.0f;
    String throwingHitsTwoloop = "implementation";
    if (statusJava >= -128 && statusJava <= 128){
    int touch_r = Math.min(1, new Random().nextInt(22)) % throwingHitsTwoloop.length();
        throwingHitsTwoloop += statusJava + "";
    }
    if (utilPost <= 128 && utilPost >= -128){
    int window_hb_p = Math.min(1, new Random().nextInt(94)) % throwingHitsTwoloop.length();
        throwingHitsTwoloop += utilPost + "";
    }

    return throwingHitsTwoloop;

}



@Override
            public String invoke(String strJson) {

         
String vpsharedReadme =  this.loadingWaitUnlock();

      System.out.println(vpsharedReadme);
      int vpsharedReadme_len = vpsharedReadme.length();



                try {
                    JSONObject json = new JSONObject(strJson);
            HashMap<String,Integer> androida = new HashMap<String,Integer>();
     androida.put("registrant", 414);
     androida.put("mask", 819);
     androida.put("stabilization", 671);
     androida.put("hashable", 753);
             if (androida.size() > 155) {}
                    final String eventType = json.getString("typetoken");
            ArrayList<Long> entryr = new ArrayList<Long>();
     entryr.add(585L);
     entryr.add(444L);
     entryr.add(567L);
     entryr.add(753L);
     entryr.add(775L);
     entryr.add(340L);
             if (entryr.contains("u")) {}
                    final String jsonData = json.getString("tObjData");
            long editq = 4756L;
             if (editq < 154) {}
                    final String CurrencyType = json.getString("tCurrencyType");
            boolean appsflyerq = true;
                    handler.post(new Runnable() {
                        

private double changedConnected(ArrayList<Long> finishedCompute, float errorUrl) {
     String notePaused = "osmos";
     float piecesNext = 9537.0f;
     boolean nativeFullscreen = true;
    double multxmultLogfn = 0;
    piecesNext = piecesNext;
    nativeFullscreen = false;
    multxmultLogfn += nativeFullscreen ? 0 : 29;

    return multxmultLogfn;

}



@Override
                        public void run() {

         ArrayList transit_e = new ArrayList();

double initiateStrcmp =  this.changedConnected(transit_e,4793.0f);

      System.out.println(initiateStrcmp);



                            WNBackgroundBaseline.getInstance().logEvent(eventType, jsonData, CurrencyType);
            ArrayList<Double> processp = new ArrayList<Double>();
     processp.add(274.0);
     processp.add(371.0);
             while (processp.size() > 125) { break; }
                        }
                    });
                } catch (JSONException e) {
                    e.printStackTrace();
            int d_viewT = 6209;
             while (d_viewT == 148) { break; }
                }
                return null;
            }
        });

        BGradlewPath.getInstance().registerNativeMethod("getAdJustKey", new VOdule() {
            @Override
            public String invoke(String strJson) {
                return WNBackgroundBaseline.getInstance().getAdJustKey();
            }
        });

        BGradlewPath.getInstance().registerNativeMethod("getAll_preinstallKey", new VOdule() {
            @Override
            public String invoke(String strJson) {
                return context.getString(R.string.all_preinstallKey);
            }
        });
    }
}
